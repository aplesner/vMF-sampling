# Sweep configuration for vMF sampling experiments
# Parameter ranges for comprehensive testing

# Parameter sweep ranges  
dimensions: [500, 1000, 5000, 10000, 20000, 100000]
kappas: [100, 500, 1000, 5000, 10000, 20000, 50000, 100000]

# Override defaults for sweep experiments
num_samples: 2000
dtype: "float16"  # Use float16 to save memory for large dimensions
output_dir: "sweep_samples/"